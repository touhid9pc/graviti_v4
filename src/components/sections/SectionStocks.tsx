import React from "react";
import Dock from "../dock/Dock";

const SectionStocks = () => {
  return (
    <div className="bg-[#FAF9F6] flex justify-center items-center w-screen h-screen">
      <Dock />
    </div>
  );
};

export default SectionStocks;
